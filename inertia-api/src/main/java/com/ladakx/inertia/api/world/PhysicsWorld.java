package com.ladakx.inertia.api.world;

/**
 * Represents the physical simulation world in Inertia.
 * It provides methods to interact with the global state of the physics engine.
 */
public interface PhysicsWorld {
    // We will add methods here in the future, such as:
    // - getBody(UUID)
    // - createBody(BodySettings)
    // - raycast(...)
    // - etc.
}
