package com.ladakx.inertia.files;

import com.ladakx.inertia.InertiaPlugin;

public class RenderFile extends CustomConfiguration {

    public RenderFile(InertiaPlugin plugin) {
        super(plugin, "render.yml");
    }
}