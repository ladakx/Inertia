package com.ladakx.inertia.files;

import com.ladakx.inertia.InertiaPlugin;

public class BlocksFile extends CustomConfiguration {

    public BlocksFile(InertiaPlugin plugin) {
        super(plugin, "blocks.yml");
    }
}